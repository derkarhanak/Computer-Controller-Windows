<Page
    x:Class="ComputerController.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:ComputerController.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Padding="24,20" Spacing="20" MaxWidth="900">
            
            <!-- Header -->
            <StackPanel Orientation="Horizontal" Spacing="16">
                <Button x:Name="BackButton" Click="Back_Click" Background="Transparent" BorderThickness="0" Padding="8" ToolTipService.ToolTip="Go Back">
                    <FontIcon Glyph="&#xE72B;" FontSize="16" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </Button>
                <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                    <FontIcon Glyph="&#xE713;" FontSize="24" Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>
                    <TextBlock Text="Settings &amp; File Operations" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
            </StackPanel>

            <!-- AI Provider Configuration -->
            <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="20" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <StackPanel Spacing="16">
                    <TextBlock Text="AI Provider Configuration" FontSize="16" FontWeight="SemiBold"/>
                    
                    <!-- Provider Selection Header -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Grid.Column="0" Text="Select AI Provider" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                        
                        <Border Grid.Column="1" Background="{ThemeResource AccentFillColorDefaultBrush}" Opacity="0.1" CornerRadius="8" Padding="8,4">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <Ellipse Width="6" Height="6" Fill="{ThemeResource AccentFillColorDefaultBrush}" VerticalAlignment="Center"/>
                                <TextBlock x:Name="CurrentProviderText" FontSize="12" FontWeight="Medium" Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <!-- Provider Buttons Grid -->
                    <GridView x:Name="ProviderGrid" SelectionMode="None" IsItemClickEnabled="True" ItemClick="ProviderGrid_ItemClick">
                        <GridView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <ItemsWrapGrid Orientation="Horizontal" MaximumRowsOrColumns="4"/>
                            </ItemsPanelTemplate>
                        </GridView.ItemsPanel>
                        <GridView.ItemTemplate>
                            <DataTemplate x:DataType="models:LLMProvider">
                                <Grid Width="160" Height="100" Padding="4">
                                    <Border x:Name="ProviderButton" CornerRadius="8" BorderThickness="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" Padding="12">
                                        <Grid>
                                            <StackPanel Spacing="8" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                <FontIcon x:Name="ProviderIcon" FontSize="28"/>
                                                <TextBlock x:Name="ProviderName" FontSize="13" FontWeight="Medium" TextAlignment="Center" TextWrapping="Wrap"/>
                                            </StackPanel>
                                            <Border x:Name="LocalBadge" Background="#2000B050" CornerRadius="4" Padding="6,2" HorizontalAlignment="Right" VerticalAlignment="Top" Visibility="Collapsed" Margin="-4,-4,-4,0">
                                                <TextBlock Text="Local" FontSize="10" Foreground="#007030" FontWeight="Bold"/>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </GridView.ItemTemplate>
                    </GridView>

                    <!-- API Key Status -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Spacing="4">
                            <TextBlock Text="API Key Status" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <Ellipse x:Name="ApiKeyStatusIndicator" Width="8" Height="8" VerticalAlignment="Center"/>
                                <TextBlock x:Name="ApiKeyStatusText" FontSize="14"/>
                            </StackPanel>
                        </StackPanel>

                        <Button x:Name="SetApiKeyButton" Grid.Column="1" Click="SetApiKey_Click" VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE8D7;" FontSize="14"/>
                                <TextBlock x:Name="ApiKeyButtonText" Text="Set Key"/>
                            </StackPanel>
                        </Button>
                    </Grid>

                    <!-- API Key Info -->
                    <InfoBar x:Name="ApiKeyInfoBar" IsOpen="True" IsClosable="False" Severity="Informational"/>

                    <!-- Model Selection (for Groq/Ollama) -->
                    <StackPanel x:Name="ModelSelectionPanel" Spacing="8" Visibility="Collapsed">
                        <TextBlock Text="Select Model:" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <ComboBox x:Name="ModelComboBox" MinWidth="200" SelectionChanged="ModelComboBox_SelectionChanged"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Appearance -->
            <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="20" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <StackPanel Spacing="16">
                    <TextBlock Text="Appearance" FontSize="16" FontWeight="SemiBold"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE706;" FontSize="20" x:Name="LightIcon"/>
                            <TextBlock Text="Light" FontSize="14" x:Name="LightText" VerticalAlignment="Center"/>
                        </StackPanel>

                        <ToggleSwitch Grid.Column="2" x:Name="ThemeToggle" Toggled="ThemeToggle_Toggled" HorizontalAlignment="Center"/>

                        <StackPanel Grid.Column="4" Orientation="Horizontal" Spacing="8">
                            <TextBlock Text="Dark" FontSize="14" x:Name="DarkText" VerticalAlignment="Center"/>
                            <FontIcon Glyph="&#xE708;" FontSize="20" x:Name="DarkIcon"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Quick File Operations -->
            <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="20" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <StackPanel Spacing="16">
                    <TextBlock Text="Quick File Operations" FontSize="16" FontWeight="SemiBold"/>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Button Grid.Column="0" Click="OpenDirectory_Click" Tag="Downloads" Margin="4" HorizontalAlignment="Stretch">
                            <StackPanel Spacing="8" Padding="8">
                                <FontIcon Glyph="&#xE896;" FontSize="24" Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>
                                <TextBlock Text="Open Downloads" FontSize="12" TextAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Button Grid.Column="1" Click="OpenDirectory_Click" Tag="Desktop" Margin="4" HorizontalAlignment="Stretch">
                            <StackPanel Spacing="8" Padding="8">
                                <FontIcon Glyph="&#xE7F4;" FontSize="24" Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>
                                <TextBlock Text="Open Desktop" FontSize="12" TextAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Button Grid.Column="2" Click="OpenDirectory_Click" Tag="Documents" Margin="4" HorizontalAlignment="Stretch">
                            <StackPanel Spacing="8" Padding="8">
                                <FontIcon Glyph="&#xE8A5;" FontSize="24" Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>
                                <TextBlock Text="Open Documents" FontSize="12" TextAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Button Grid.Column="3" Click="OpenDirectory_Click" Tag="Pictures" Margin="4" HorizontalAlignment="Stretch">
                            <StackPanel Spacing="8" Padding="8">
                                <FontIcon Glyph="&#xEB9F;" FontSize="24" Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>
                                <TextBlock Text="Open Pictures" FontSize="12" TextAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Example Commands -->
            <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="24" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <StackPanel Spacing="16">
                    <TextBlock Text="Example Commands" FontSize="18" FontWeight="SemiBold"/>
                    
                    <StackPanel Spacing="12">
                        <Button HorizontalAlignment="Stretch" Padding="0" BorderThickness="0" Background="Transparent" Click="ExampleCommand_Click" Tag="Move PDFs">
                            <Border Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16" HorizontalAlignment="Stretch">
                                <Grid ColumnDefinitions="Auto,*">
                                    <FontIcon Glyph="&#xE8B7;" FontSize="20" Foreground="{ThemeResource AccentFillColorDefaultBrush}" VerticalAlignment="Center" Margin="0,0,16,0"/>
                                    <StackPanel Grid.Column="1" Spacing="4">
                                        <TextBlock Text="Move all PDF files from Downloads to Documents" FontSize="14" FontWeight="Medium"/>
                                        <TextBlock Text="Organize your files automatically by file type" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </Button>

                        <Button HorizontalAlignment="Stretch" Padding="0" BorderThickness="0" Background="Transparent" Click="ExampleCommand_Click" Tag="Rename Images">
                            <Border Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16" HorizontalAlignment="Stretch">
                                <Grid ColumnDefinitions="Auto,*">
                                    <FontIcon Glyph="&#xE8B9;" FontSize="20" Foreground="{ThemeResource AccentFillColorDefaultBrush}" VerticalAlignment="Center" Margin="0,0,16,0"/>
                                    <StackPanel Grid.Column="1" Spacing="4">
                                        <TextBlock Text="Rename all image files in Pictures folder" FontSize="14" FontWeight="Medium"/>
                                        <TextBlock Text="Batch rename with date prefixes for better organization" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </Button>

                        <Button HorizontalAlignment="Stretch" Padding="0" BorderThickness="0" Background="Transparent" Click="ExampleCommand_Click" Tag="Backup Files">
                            <Border Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16" HorizontalAlignment="Stretch">
                                <Grid ColumnDefinitions="Auto,*">
                                    <FontIcon Glyph="&#xEA35;" FontSize="20" Foreground="{ThemeResource AccentFillColorDefaultBrush}" VerticalAlignment="Center" Margin="0,0,16,0"/>
                                    <StackPanel Grid.Column="1" Spacing="4">
                                        <TextBlock Text="Create a backup folder and copy important files" FontSize="14" FontWeight="Medium"/>
                                        <TextBlock Text="Set up automated backups for critical documents" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </Button>

                        <Button HorizontalAlignment="Stretch" Padding="0" BorderThickness="0" Background="Transparent" Click="ExampleCommand_Click" Tag="Clean Downloads">
                            <Border Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="8" Padding="16" HorizontalAlignment="Stretch">
                                <Grid ColumnDefinitions="Auto,*">
                                    <FontIcon Glyph="&#xE74D;" FontSize="20" Foreground="{ThemeResource AccentFillColorDefaultBrush}" VerticalAlignment="Center" Margin="0,0,16,0"/>
                                    <StackPanel Grid.Column="1" Spacing="4">
                                        <TextBlock Text="Clean up empty folders in Downloads" FontSize="14" FontWeight="Medium"/>
                                        <TextBlock Text="Remove unnecessary directories to keep things tidy" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
